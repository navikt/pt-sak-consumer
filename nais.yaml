image: navikt/pt-sak-consumer
team: di
replicas:
  min: 1
  max: 1
secrets: true
healthcheck:
  liveness:
    path: /
    initialDelay: 20
    timeout: 1
    periodSeconds: 5
    failureThreshold: 10
  readiness:
    path: /
    initialDelay: 20
    timeout: 1
prometheus:
  enabled: true
  path: /
